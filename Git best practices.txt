Pull lastest chenages to branch

git pull --rebase

Create a new story/ defect branch

git checkout -b 'branch name'

On branch merge into master

git pull --rebase

git checkout master

git pull --rebase

git merge --no-ff 'branch name'

git commit --amend

git push



